const API_HOST = "http://localhost:8000";

export const BOOKS = `${API_HOST}/books/`;
export const MEMBERS = `${API_HOST}/members/`;

const ORDER = `${API_HOST}/order/`;
export const CHECKOUT = `${ORDER}checkout/`;
export const RETURN = `${ORDER}return/`;
